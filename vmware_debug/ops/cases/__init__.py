"""Investigation case state — the ledger the eight-step evidence loop writes to.

See ``docs/plans/2026-08-28-investigation-layer-design.md``. The case directory
is the deliverable, not an implementation detail: everything except the index
is human-readable text, so a customer can take the folder away and audit how a
conclusion was reached without any VMware environment or any of these tools.
"""
